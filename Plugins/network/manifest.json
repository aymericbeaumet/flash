{
  "id": "network",
  "name": "Network",
  "version": "0.1.0",
  "description": "Publish a fixed-width network label with traffic history on hover; summary_mode=full restores rates and charts inline, alongside copyable @network.addresses.",
  "sandbox": {
    "exec": [
      "/usr/sbin/netstat"
    ]
  },
  "exec": [
    "./flash-plugin-network"
  ],
  "commands": [
    {
      "command": "network",
      "subcommand": "",
      "description": "Show current network traffic and addresses"
    },
    {
      "command": "network",
      "subcommand": "refresh",
      "description": "Refresh network traffic, routing table, and addresses"
    }
  ],
  "status": [
    "summary",
    "details"
  ],
  "sources": [
    {
      "name": "network.addresses",
      "priority": "low"
    }
  ]
}
