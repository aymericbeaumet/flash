{
  "contract": "docs/plugin-protocol.md#framing",
  "description": "An inbound line over the 10 MiB cap is dropped — never fatal — and the stream self-heals at the next newline. The 11 MiB junk line (sent without its newline first, then terminated) must not kill the plugin or poison the framing: the follow-up ping still answers.",
  "timeout_ms": 60000,
  "steps": [
    {
      "send": { "id": 1, "method": "initialize", "params": { "protocol_version": 1 } }
    },
    {
      "expect": { "id": 1, "result": { "ok": true, "protocol_version": 1 } },
      "within_ms": 5000
    },
    { "send_raw": { "$repeat": { "value": "x", "count": 11534336 } } },
    { "send_raw": "\n" },
    { "send": { "id": 4, "method": "ping", "params": {} } },
    { "expect": { "id": 4, "result": { "ok": true } }, "within_ms": 15000 },
    { "close_stdin": true },
    { "expect_exit": { "code": 0 }, "within_ms": 3000 }
  ]
}
