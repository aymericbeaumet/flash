{
  "id": "cpu",
  "name": "CPU & GPU",
  "version": "0.1.0",
  "description": "Publish one-second CPU snapshots with periodically refreshed GPU telemetry and structured hover details; summary_mode=full restores secondary telemetry inline.",
  "exec": [
    "./flash-plugin-cpu"
  ],
  "commands": [
    {
      "command": "cpu",
      "subcommand": "",
      "description": "Show the latest CPU and GPU snapshot"
    },
    {
      "command": "cpu",
      "subcommand": "refresh",
      "description": "Refresh CPU and GPU status immediately"
    }
  ],
  "status": [
    "summary",
    "details"
  ],
  "sandbox": {
    "exec": [
      "/usr/sbin/ioreg"
    ]
  }
}
