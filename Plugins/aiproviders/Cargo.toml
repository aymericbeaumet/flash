[package]
name = "flash-plugin-aiproviders"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "flash-plugin-aiproviders"
path = "src/main.rs"

[dependencies]
flash_plugin = { path = "../_flash_plugin_rust" }
serde_json = "1"
tokio = { version = "1", default-features = false, features = [
  "rt",
  "macros",
  "io-std",
  "io-util",
  "fs",
  "process",
  "time",
  "sync",
] }

[profile.plugin-dev]
inherits = "dev"
opt-level = 1
debug = "line-tables-only"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
