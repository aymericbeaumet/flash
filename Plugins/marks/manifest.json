{
  "id": "marks",
  "name": "Marks",
  "version": "0.1.0",
  "description": "Named app marks with explicit commands or custom mappings. Marks persist across restarts.",
  "sandbox": {},
  "exec": [
    "./flash-plugin-marks"
  ],
  "capabilities": [
    "app_control"
  ],
  "verbs": [
    {
      "name": "set_mark",
      "description": "Save the focused non-Flash app under a one-letter slot (a-z)."
    },
    {
      "name": "jump_to_mark",
      "description": "Reactivate the app previously saved under the given letter."
    }
  ],
  "help": {
    "topics": [
      {
        "name": "marks",
        "title": "Marks",
        "summary": "Save and recall named apps with explicit commands or custom mappings.",
        "body": "# Marks\n\nEach letter `a`-`z` holds one mark. Setting a mark records the focused app's pid and bundle identifier so jumps survive process restarts.\n\n## Commands\n\n```text\nflash set_mark --letter=a\nflash jump_to_mark --letter=a\n```\n\nIf the original pid is gone, Flash relaunches the app from its bundle identifier.\n\n## Optional mappings\n\nMarks have no default NORMAL bindings. Choose explicit mappings in `flash.toml`:\n\n```toml\n[mode.normal.mappings]\n\"ma\" = [\"flash\", \"set_mark\", \"--letter=a\"]\n\"`a\" = [\"flash\", \"jump_to_mark\", \"--letter=a\"]\n```\n\n## Storage\n\nThe 26 lowercase slots persist to `Library/Application Support/Flash/Plugins/marks/share/marks.json`.\n",
        "aliases": [
          "mark"
        ]
      }
    ]
  },
  "listen": [
    "core:flash.started",
    "core:apps.changed",
    "core:apps.launched",
    "core:apps.terminated"
  ]
}
