{
  "id": "clipboard",
  "name": "Clipboard",
  "version": "0.1.0",
  "description": "Clipboard history in flashlight; pick an entry to paste it. Plus :clipboard clear.",
  "install": "true",
  "start": "exec ./flash-plugin-clipboard",
  "commands": [
    { "command": "clipboard", "subcommand": "clear", "description": "Clear the clipboard history" }
  ]
}
