# Flash Help

`:help` opens the in-app topic index.

`:help <topic>` opens a specific topic, for example:

```text
:help plugins
:help normal-mode
:help urls
:help config
```

Runtime help topic text is kept in Swift source files next to the feature code
that owns each topic.
