# Python drives the language-neutral protocol conformance runner. Official
# plugins are Rust crates built with rustup-managed Cargo; build-plugins.sh
# uses rustup's multi-target support for universal release builds.
[tools]
python = "3.13"
